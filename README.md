# WEBNEST RESTAURANT

React + Vite restaurant ordering website.

## Run locally
npm install
npm run dev

## Netlify
Build command: npm run build
Publish directory: dist

The project intentionally uses plain CSS instead of Tailwind so there are no Tailwind content/configuration issues.
